usestate sert juste au variable du composant lui meme
C'est comme une attribut locale exemple private int age

usestate te ramene la derniere version


usestate a pas besoin de useeffect
mais useeffect a besoin de usestate
le link recherche une route existante avec le path

useeffect seulement quand ca vient de l'exterieur
c'est une fonction native de react.
permet de faire des calls http
il gere les appelles webservices
et gere les changements

c'est comme si tu fais un select, et la table change et ta pas besoin de refaire un select ta les bonnes donnes

axios c'est crud 

then catch c'est un try catch

difference axios et fetch

fetch ne marche pas sur tous les browser, ex ancien browser
axios c'est une version reduite/code reduit de fetch 
fetch est native de js
axios permet de ne pas specifier le json
parce qu'il detecte ce que tu recois


difference entre useeffect et usestate 
pas de difference il font pas dutout la meme chose

